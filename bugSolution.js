```javascript
// Check your postcss.config.js to make sure tailwindcss is included correctly
// Check your purge options in tailwind.config.js to make sure the class is being included in the build
// Rebuild the project to ensure the changes are applied
<div class="bg-red-500 hover:bg-blue-700">
  <p>This should be red, and blue on hover.</p>
</div>
```